enum JMClientRPC
{
	INVALID = 10100,
	RefreshClients,
	RemoveClient,
	UpdateClient,
	SetClient,
	COUNT
};

enum JMRoleRPC
{
	INVALID = 10120,
	RefreshRoles,
	UpdateRole,
	RemoveRole,
	COUNT
};

enum JMPermissionRPC
{
	INVALID = 10140,
	COUNT
};