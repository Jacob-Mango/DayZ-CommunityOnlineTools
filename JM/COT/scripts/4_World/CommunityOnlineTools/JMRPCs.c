enum JMClientRPC
{
    INVALID = 10100,
    RefreshClients,
    RemoveClient,
    UpdateClient,
    SetClient,
    MAX
}

enum JMRoleRPC
{
    INVALID = 10200,
    RefreshRoles,
    UpdateRole,
    RemoveRole,
    MAX
}

enum JMPermissionRPC
{
    INVALID = 10300,
    MAX
}